[tool.poetry]
name = "paperscissorstone"
version = "0.1.0"
description = ""
authors = ["Miyago"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
python-discord = "^1.7.3"
python-dotenv = "^1.0.0"
discord-py = "^2.3.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
